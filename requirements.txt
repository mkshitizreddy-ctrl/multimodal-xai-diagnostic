# Core
# NOTE: torch/torchvision below install the CPU build from PyPI (needed for
# CI, which has no GPU). If you're on a machine with an NVIDIA GPU, install
# the CUDA build separately per https://pytorch.org/get-started/locally/
# (this repo was developed against the +cu121 build).
torch==2.5.1
PyYAML==6.0.3
torchvision==0.20.1
numpy==2.4.4
pandas==3.0.5
scikit-learn==1.9.0

# Imaging / DICOM
pydicom==3.0.2
kagglehub==1.0.2
huggingface_hub==1.26.1
Pillow==12.2.0
opencv-python-headless==5.0.0.93

# Explainability
grad-cam==1.5.5
torchxrayvision==1.5.2

# Dashboard
streamlit==1.60.0
plotly==6.9.0

# Experiment tracking
tqdm==4.70.0
matplotlib==3.11.1
jupyter==1.1.1

# Testing / linting
pytest==9.1.1
ruff==0.16.2
